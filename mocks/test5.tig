/* define valid recursive types */
let
	type IntList = {head: int, next: IntList}
	type Tree = {key: int, children: Tree}
	var ls = {head: 0, next: nil}IntList
	var t = {key: 4, children: nil}Tree
in
	ls; t
end
