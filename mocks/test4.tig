/* define a recursive function */
/*let*/

/* calculate n! */
/*
function nfactor(n: int): int =
		if  n = 0 
			then 1
			else n * nfactor(n-1)

in
	nfactor(10)
end
*/

let

function factor(n: int) {
	/*n == 0 ? 1 : n * factor(n - 1)*/
	if n == 0 then 1 else 2 end;
	if n == 0 then 1 else n * factor(n - 1) end
}

function factor5 = factor(5)

in
	1
end
