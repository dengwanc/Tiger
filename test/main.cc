#include <iostream>

using namespace std;

int main() {
  cout << "\n--------- TESTS PASSING -----------\n" << endl;
}

/**
 * Notice that this tiny
 * unit test framework,
 * depend on c++ init static var
 * in compiling time.
 * So that those *.spec.cc file
 * exec is unordered, Be Careful!
 */
